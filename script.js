const launch = (domain) =>{

    // window.location.href = "https://www." + domain
}

let githubBtn = document.getElementById('github-btn')

githubBtn.addEventListener('click', launch("github.com/joyloruth"))

